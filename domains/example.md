<!--
This directory holds one JSON file per subdomain — the file name is the
subdomain. See ../README.md and ../CONTRIBUTING.md for the full guide.

A minimal file (domains/<your-name>.json) looks like:

{
  "owner": {
    "username": "your-github-username",
    "email": "you@example.com"
  },
  "records": {
    "CNAME": "your-github-username.github.io"
  }
}

This .md file is ignored by the tooling (only *.json files are processed).
-->
